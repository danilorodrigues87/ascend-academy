# Usado automaticamente por `npm run build` (Vite mode=production).
# NÃO use localhost aqui — o navegador do aluno não alcança o seu PC.

VITE_API_BASE_URL=https://admin.ctieducacional.com.br/api/v1/student
