# Em desenvolvimento: URL relativa + proxy do Vite (evita Failed to fetch / CORS)
VITE_API_BASE_URL=/api/v1/student

# Destino Apache do painel-cti (só para o proxy do vite.config)
# VITE_PAINEL_PROXY_TARGET=http://localhost/pjt/painel-cti
