# Desenvolvimento: URL relativa + proxy do Vite
VITE_API_BASE_URL=/api/v1/student

# Destino Apache do painel-cti (só para o proxy do vite.config)
# VITE_PAINEL_PROXY_TARGET=http://localhost/pjt/painel-cti

# Produção: use o arquivo .env.production (carregado no `npm run build`)
# VITE_API_BASE_URL=https://admin.ctieducacional.com.br/api/v1/student
